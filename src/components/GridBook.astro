<div class="grid">
  <slot />
</div>

<style>
  .grid {
    display: grid;
    gap: 20px;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    padding-left: 7vw;
    padding-right: 7vw;
    place-content: center;
  }
</style>
