<div class="grid">
  <slot />
</div>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 20px;
    place-content: center;
    padding-left: 7vw;
    padding-right: 7vw;
  }
</style>
